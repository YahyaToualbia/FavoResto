import 'package:flutter/material.dart';

/// Defines the color pallete for the app UI kit.
abstract class AppColors {
  /// the primary white color.
  static const primaryWhite = Color(0xFFFDFAF6);

  /// the secondary white color.
  static const secondaryWhite = Color(0xFFE4EFE7);

  /// the neutral white color.
  static const neutralWhite = Color.fromARGB(255, 255, 247, 236);

  /// the light green color.
  static const lighGreen = Color(0xFF99BC85);

  /// The light green color with opacity.
  static const lightGreenWithOpacity = Color(0x299CFF63);

  /// the black color
  static const black = Color(0xFF000000);

  /// the Grey color.
  static const Color grey = Color(0xFFABB7C2);

  /// The background color.
  static const Color backgroundColor = Color(0xFFFAF1E6);

  /// The primary Color
  static const Color primaryColor = Colors.black;

  /// The purple color.
  static const Color purple = Color(0xFF9529A7);

  /// The light purple color.
  static const Color lightPurple = Color(0xFFD7AEDE);

  /// The yellow color
  static const yellow = Color(0xFFE3BB64);

  /// The light yellow color.
  static const lightYellow = Color(0xFFF3E1BC);

  /// The blue color.
  static const blue = Color(0xFF4CCCEC);

  /// The dark blue color.
  static const darkerkBlue = Color.fromARGB(255, 114, 158, 255);

  /// The light blue color.
  static const lightBlue = Color(0xFFC2EDF8);
}
